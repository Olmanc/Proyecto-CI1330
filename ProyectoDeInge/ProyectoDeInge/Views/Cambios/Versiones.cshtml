@model IEnumerable<ProyectoDeInge.Models.CAMBIOS>

@{
    ViewBag.Title = "Versiones";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Historial de Versiones</h2>

@*@using (Html.BeginForm())
    {
        <p>
            @Html.DropDownList("pro", null, htmlAttributes: new { @class = "form-control", onchange = "this.form.submit()" })
        </p>
    }*@

<table class="table">

    <tr>

        <th>
            ID de Cambio
        </th>
        <th>
            ID de Requerimiento
        </th>
        <th>
            Version
        </th>
        <th>
            Fecha
        </th>
        <th>
            Descripcion
        </th>
        <th>
            Justificacion
        </th>
        <th>
            Cedula
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NUEVO_REQ_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NUEVO_VER_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FECHA)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DESCRIPCION)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JUSTIFICACION)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CEDULA)
            </td>
            <td>
                @Html.ActionLink("Consultar", "ConsultarVers", new { id = item.ID })
            </td>
        </tr>
    }
</table>
